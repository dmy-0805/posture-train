/* 启动入口：挂钩云同步 → 初始化应用 → 拉取云端数据合并 */
(function () {
  Sync.hook();   // 包装 save 方法（必须在 init 之前，init 内部也会触发保存）
  App.init();    // 加载本地数据并渲染
  Sync.start();  // 拉取云端并与本地合并，之后进入自动推送模式
  Sync.bind();   // 监听网络状态，断网/恢复自动处理
})();
